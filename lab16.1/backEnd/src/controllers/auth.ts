import type { Request, Response, NextFunction } from 'express'

import moduleName from '../models/mongooseModels/User.js'

function login(req: Request, res: Response, next: NextFunction) {
    
}


export default { login }